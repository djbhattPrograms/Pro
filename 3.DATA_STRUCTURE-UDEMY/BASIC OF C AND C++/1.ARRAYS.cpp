#include <iostream>
#include <stdio.h>
using namespace std;

int main()
{
    // int A[5] = {4, 5, 2, 4, 5};
    // for (int i = 0; i < 5; i++)
    // {
    //     cout << "Array Element-" << i << ": " << A[i] << endl;
    // }
    // // for each loop
    // for (int i : A)
    // {
    //     cout<<i<<endl;
    // }
    // creating a desire arrays
    int size;
    cout << "Enter a size of Array" << endl;
    cin >> size;
    int b[size];
    for (int i = 0; i < sii++)
    {ze; 
        cin >> b[i];
    }
    for (int i = 0; i < size; i++)
    {
        cout << "Array Element-" << i << ": " << b[i] << endl;
    }

     // printf("%d\n", A[2]);
    // cout << "Size of the Array in the stack of the memory: " << sizeof(A) << endl;

    return 0;
}